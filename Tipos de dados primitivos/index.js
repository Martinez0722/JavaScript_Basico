// String, Number, Undefined, Null, Boolean

const nome = 'Rafael';// String
const nome1 = "Rafael";// String
const nome2 =`Rafael`;// String para template string
const num1 = 10; //Number
const num2 = 10.52;//Number
let nomeAluno; // undefined -> não aponta para nenhum local na memória automaticamente configurado
const sobrenomeAluno = null; // Nulo -> não aponta para nenhum local na memória manualmente/intencionalmente configurado
const aprovado = false; // Boolean = true, false (lógico)

